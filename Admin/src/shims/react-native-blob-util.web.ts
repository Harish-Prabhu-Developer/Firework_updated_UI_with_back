const ReactNativeBlobUtil = {
  fs: {
    dirs: {
      DownloadDir: '',
    },
    writeFile: async () => {},
    createFile: async () => {},
  },
  android: {
    addCompleteDownload: () => {},
  },
  config: () => ({
    fetch: async () => ({}),
  }),
};

export default ReactNativeBlobUtil;
